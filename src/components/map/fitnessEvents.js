// Shared by the home page map and the standalone map component. City pins
// and their events come from src/data/fitnessEvents.js, which also backs the
// /events pages, so the map and the listings can never disagree.
export { citySummaries } from '../../data/fitnessEvents';

export const INDIA_CENTER = [22.5, 80.5];
export const INDIA_ZOOM = 4;
